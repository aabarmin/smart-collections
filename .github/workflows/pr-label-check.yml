name: Check PR Labels

on:
  pull_request:
    branches:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check PR labels
      run: echo "1234"
      env:
        ${{ secrets.GITHUB_TOKEN }}
      