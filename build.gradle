plugins {
	id 'idea'
}

group = 'dev.abarmin'
version = '0.0.1-SNAPSHOT'

subprojects {
	repositories {
		mavenCentral()
	}

	apply {
		plugin 'java'
	}

	java {
		sourceCompatibility = JavaVersion.VERSION_17
	}

	tasks.named('test') {
		useJUnitPlatform()
	}
}
